import styled from "styled-components";

export const StyledAbout = styled.div`

    position: relative;

    ul{
        list-style: none;
    }

    .waveIMG{
        transform: rotate(180deg);
        position: absolute;
        top: 0;
        z-index: -1;
    }

    .wrapper{
        z-index: 444;
        display: flex;
        
    }


`